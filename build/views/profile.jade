extends layout

block content
    .row
        .col-xs-12
            h1 Profile
    .row
        .col-xs-12
            h3 #{userObj.first_name} #{userObj.last_name}
        .col-xs-12
            p T#: #{userObj.t_number}
        .col-xs-12
            p E: #{userObj.email}
        .col-xs-12
            p Type: #{userObj.privileged}